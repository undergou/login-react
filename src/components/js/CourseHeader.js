import React from 'react';

export default class CourseHeader extends React.Component {
    render() {
        return (
            <div className="course-header">
                <h2>Courses</h2>
                <p>Now the platform contains more than 2000 tasks</p>
            </div>
        );
    }
}